/**
 * Header view (if you want a header) with links to switch page.
 */
import React, { PureComponent } from 'react';

export default class Applications extends PureComponent {

  render() {
    return (
      <div style={ { paddingTop: '90px' } }>This is a test</div>
    );
  }
}
